[project]
name = "facts-module-registry"
version = "0.1.0"
description = "Module registry for FACTS v2 sea-level rise experiments"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
include = ["src/facts_module_registry"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
